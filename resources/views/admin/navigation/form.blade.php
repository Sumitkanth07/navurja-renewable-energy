@extends('layouts.admin')
@section('content')<h1>{{ $item->exists ? 'Edit Navigation' : 'Add Navigation' }}</h1><form class="panel form" method="post" action="{{ $item->exists ? route('admin.navigation.update',$item) : route('admin.navigation.store') }}">@csrf @if($item->exists)@method('put')@endif<label>Label<input name="label" value="{{ old('label',$item->label) }}" required></label><label>URL<input name="url" value="{{ old('url',$item->url) }}" placeholder="#about or /blog" required></label><label>Sort order<input type="number" name="sort_order" value="{{ old('sort_order',$item->sort_order ?? 0) }}"></label><label class="check"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$item->is_active ?? true))> Active</label><button>Save Navigation</button></form>@endsection
