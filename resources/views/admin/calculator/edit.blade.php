@extends('layouts.admin')
@section('content')<h1>Calculator Settings</h1><form class="panel form" method="post" action="{{ route('admin.calculator.update') }}">@csrf @method('put')<label>Cost per kW<input type="number" step="0.01" name="cost_per_kw" value="{{ old('cost_per_kw',$settings->cost_per_kw ?? 55000) }}"></label><label>Monthly savings rate<input type="number" step="0.01" name="monthly_savings_rate" value="{{ old('monthly_savings_rate',$settings->monthly_savings_rate ?? 0.82) }}"></label><label>CO2 per kW per year<input type="number" step="0.01" name="co2_per_kw_year" value="{{ old('co2_per_kw_year',$settings->co2_per_kw_year ?? 1.25) }}"></label><label>Cities, one per line as City | sun factor<textarea name="cities" rows="8">@foreach($cities as $city){{ $city->name }} | {{ $city->sun_factor }}
@endforeach</textarea></label><button>Save Calculator</button></form>@endsection
