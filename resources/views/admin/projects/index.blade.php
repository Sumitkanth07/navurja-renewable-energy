@extends('layouts.admin')
@section('content')<div class="topbar"><h1>Projects</h1><a class="button" href="{{ route('admin.projects.create') }}">Add Project</a></div><table><tr><th>Title</th><th>Status</th><th></th></tr>@foreach($projects as $project)<tr><td>{{ $project->title }}</td><td>{{ $project->is_active ? 'Active' : 'Hidden' }}</td><td><a href="{{ route('admin.projects.edit',$project) }}">Edit</a><form method="post" action="{{ route('admin.projects.destroy',$project) }}">@csrf @method('delete')<button>Delete</button></form></td></tr>@endforeach</table>{{ $projects->links() }}@endsection
