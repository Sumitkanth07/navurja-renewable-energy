@extends('layouts.admin')
@section('content')<h1>{{ $project->exists ? 'Edit Project' : 'Add Project' }}</h1><form class="panel form" method="post" enctype="multipart/form-data" action="{{ $project->exists ? route('admin.projects.update',$project) : route('admin.projects.store') }}">@csrf @if($project->exists)@method('put')@endif<label>Title<input name="title" value="{{ old('title',$project->title) }}" required></label><label>Description<textarea name="description" required>{{ old('description',$project->description) }}</textarea></label><label>Image<input type="file" name="image">@if($project->image)<img class="preview" src="{{ asset('storage/'.$project->image) }}">@endif</label><label class="check"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$project->is_active ?? true))> Active</label><button>Save Project</button></form>@endsection
