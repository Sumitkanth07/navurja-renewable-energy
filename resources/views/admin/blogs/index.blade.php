@extends('layouts.admin')
@section('content')<div class="topbar"><h1>Blogs</h1><a class="button" href="{{ route('admin.blogs.create') }}">Add Blog</a></div><table><tr><th>Title</th><th>Category</th><th>Status</th><th></th></tr>@foreach($posts as $post)<tr><td>{{ $post->title }}</td><td>{{ optional($post->category)->name }}</td><td>{{ $post->is_published ? 'Published' : 'Draft' }}</td><td><a href="{{ route('admin.blogs.edit',$post) }}">Edit</a><form method="post" action="{{ route('admin.blogs.destroy',$post) }}">@csrf @method('delete')<button>Delete</button></form></td></tr>@endforeach</table>{{ $posts->links() }}@endsection
