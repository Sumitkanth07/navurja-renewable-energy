@extends('layouts.admin')
@section('content')<h1>{{ $redirect->exists ? 'Edit Redirect' : 'Add Redirect' }}</h1><form class="panel form" method="post" action="{{ $redirect->exists ? route('admin.redirects.update',$redirect) : route('admin.redirects.store') }}">@csrf @if($redirect->exists)@method('put')@endif<label>Old URL<input name="old_url" value="{{ old('old_url',$redirect->old_url) }}" placeholder="/old-page" required></label><label>New URL<input name="new_url" value="{{ old('new_url',$redirect->new_url) }}" placeholder="/new-page" required></label><label>Status<select name="status_code"><option value="301" @selected(old('status_code',$redirect->status_code)==301)>301 Permanent</option><option value="302" @selected(old('status_code',$redirect->status_code)==302)>302 Temporary</option></select></label><label class="check"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$redirect->is_active ?? true))> Active</label><button>Save Redirect</button></form>@endsection
